import { NavBarContainer } from "./NavBarContainer";

const NavBar = (): JSX.Element => {
  return (
    <NavBarContainer>
      <p>HACKER NEWS</p>
    </NavBarContainer>
  );
};

export default NavBar;
