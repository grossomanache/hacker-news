import styled from "styled-components";

const FilterContainer = styled.div`
  select {
    width: 240px;
    height: 32px;
    margin-left: 30px;
    size: 20;
  }
`;

export default FilterContainer;
